<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.chic_ecommerce">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".common.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChicECommerce">
        <activity
            android:name=".productactivity.presentation.ProductActivity"
            android:exported="true" >
        </activity>
        <activity
            android:name=".navigationactivity.presentation.NavigationActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".signupactivity.presentation.SignupActivity"
            android:exported="true" />
        <activity
            android:name=".loginactivity.presentation.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".mainactivity.presentation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter></activity>
    </application>

</manifest>